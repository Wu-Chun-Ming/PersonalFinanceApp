import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    text: {
        fontSize: 15,
    },
    boldText: {
        fontSize: 15,
        fontWeight: 'bold',
    },
    centered: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    centeredFlex: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});